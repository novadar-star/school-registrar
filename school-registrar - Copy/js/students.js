// students.js — client-side filter for grade and status columns
document.addEventListener('DOMContentLoaded', function () {
  // Nothing needed here — filtering is now fully server-side
  // Export is handled by the <a href="export.php"> link directly
});
